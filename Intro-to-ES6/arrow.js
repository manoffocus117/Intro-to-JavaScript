// function expression
function add(a, b) {
      // function body
      return a + b;
}

let result = add(23, 45);
// console.log(result);

// arrow function
const sum = (a, b) => {
      return a + b;
};

let output = sum(23, 22);
console.log(output);

// const multiply = (num_1, num_2) => num_1 * num_2;

const multiply = (num_1, num_2) => {
      return num_1 * num_2;
}

let mult = multiply (23,22);
console.log(mult);