// console.log("this is object in javascript");

let age = 21;
let college = "MMC";
let is_passed = false;

let is_developer;

// console.log(is_developer);

is_developer = true;

// console.log(is_developer);

let subjects = ["Bangla", "English", "Math", "Chemistry", "Physics", "History"];

// console.log(subjects);

// object is a type of data storage, where you can store multiple value with a key (property) value pair,
// to declare a object you will use a curly bracket ( {} ), 
// inside the curly bracket you can define a key (property) with a single value or a array or a object
// the key (property) value pair will separated using comma ( , )
// object is a non-primitive data type. non-primitive means mixed data

let person = {
      name: "habul",
      age: 21,
      education: "honours first year",
      is_passed: false,
      profession: "developer",
      skills: ["javascript", "typescript", "react js", "next js", "node js", "express js"],
      is_developer: true,
}

// console.log(person);

let subject = {
      name : "Math",
      teacher : "babul sir",
      exam_type : "final exam",
      exam_date : "14 feb",
      chapters : ["first", "third", "fourth", "seventh", "eighth"],
      all_chapter_completed : false,
      mark_distribution : {
            total_mark : 100,
            mcq : 30,
            written : 70,
      }
}










