// for loop in javascript
/**
 * syntax =>  
 *    for ( variable initialization; condition; increment or decrement ) {
            loop body or code that will be executed
      }
 * 
 */

for ( let index = 0; index < 10; index++ ) {
      console.log(index);
}